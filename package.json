{
  "name": "PrimeNode.js",
  "version": "0.0.1",
  "description": "Get the prime numbers between 1 and something",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tryy3",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.6.0",
    "inquirer": "^0.8.0"
  }
}
